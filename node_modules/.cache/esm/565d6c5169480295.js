let Jwt;_694‍.x([["default",()=>_694‍.o]]);_694‍.w("jsonwebtoken",[["default",["Jwt"],function(v){Jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next();
    try {
        const decoded = Jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: decoded._id,
            username: decoded.username,
        };
        _694‍.g.console.log(decoded);
        return next();
    } catch (e) {
        return next();
    }
};
_694‍.d(jwtMiddleware);
